export const isFrontBiggerThanBack = (front, back) => front > back;

export const isFrontSmallerThanBack = (front, back) => front < back;

export const isNull = value => value == null;

export const isEqual = (target, compare) => target === compare;

export const isSatisfyAnd = (front, back) => front && back;